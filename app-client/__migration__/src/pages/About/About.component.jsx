import StatelessWidget, { SIZE, TYPE } from '@/components/StatelessWidget';

function About() {
    return (
        <StatelessWidget type={TYPE.PAGE} size={SIZE.LARGE} title="About page"/>
    );
}

export default About;
