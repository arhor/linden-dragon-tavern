embedded:
  postgresql:
    enabled: true

spring:
  profiles:
    active: test

#  r2dbc:
#    url: r2dbc:tc:postgresql://${embedded.postgresql.host}:${embedded.postgresql.port}/${embedded.postgresql.database}
#    username: ${embedded.postgresql.account}
#    password: ${embedded.postgresql.password}

  flyway:
    enabled: true
    locations: classpath:/db/migration
