allprojects {
    tasks.withType(JavaCompile) {
        options.compilerArgs.addAll([
                "-Xlint:unchecked",
                "-Xlint:deprecation"
        ])
    }
}

wrapper {
    gradleVersion = "6.4.1"
}
