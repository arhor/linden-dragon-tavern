allprojects {
    tasks.withType(JavaCompile) {
        options.compilerArgs.addAll([
                '-Xlint:unchecked',
                '-Xlint:deprecation'
        ])
    }

    ext {
        set('springCloudVersion', 'Hoxton.SR3')
    }
}
