allprojects {
    tasks.withType(JavaCompile) {
        options.compilerArgs.addAll([
                "-Xlint:unchecked",
                "-Xlint:deprecation"
        ])
    }
}

wrapper {
    gradleVersion = "${gradle_version}"
}
